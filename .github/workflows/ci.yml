name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm run typecheck

      - run: pnpm run typecheck:test

      - run: pnpm test

      - run: pnpm run build

      # lib/ is committed for git-hosted installs (they never build). Fail
      # when the committed copy drifts from a clean build.
      - name: Verify committed lib/ matches the build
        shell: bash
        run: |
          if ! git diff --exit-code -- lib; then
            echo "::error::lib/ is out of sync — run 'pnpm run build' and commit the result"
            exit 1
          fi
